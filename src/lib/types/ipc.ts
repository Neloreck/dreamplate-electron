export enum EIpcChannel {
  GENERAL = "general"
}

export enum EIpcSignal {
  PING = "PING",
  PONG = "PONG",
  GET_APPLICATION_ABOUT_INFO = "GET_APPLICATION_ABOUT_INFO"
}

export enum EIpcMeta {
  HANDLERS = "HANDLERS"
}
